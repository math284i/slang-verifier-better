field f: Int

method loopframing(x: Ref, y: Ref)
    requires acc(x.f) && acc(y.f)//frame
{
    x.f := 0
    y.f := 7 //Frame
    while (x.f < 10)
        invariant acc(x.f)
    {
        x.f := x.f + 1
    }

    assert y.f == 7 //Frame
    assert perm(x.f) == 1/1

}
